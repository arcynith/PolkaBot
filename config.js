module.exports = {
    // Prefix untuk command bot
    prefix: '.',

    // Nama bot
    botName: 'Polka',

    // Author sticker
    stickerAuthor: '',

    // Max file size download (dalam MB)
    maxFileSize: 50,

    // Max durasi video untuk sticker (dalam detik)
    maxStickerDuration: 10,

    // Timeout download (dalam ms) - 3 menit
    downloadTimeout: 180000,

    // Auto-delete temp files setelah (dalam ms) - 5 menit
    tempCleanupInterval: 300000,

    // Owner number (ganti dengan nomor kamu, format: 628xxx)
    ownerNumber: '6285179901459'
};
